name: Daily DBT Job

on:
  schedule:
    - cron: "0 5 * * *" # Runs every day at 5 AM UTC
  workflow_dispatch: # Allows manual trigger

jobs:
  dbt-run:
    uses: ./.github/workflows/setup-dbt.yml # Calls setup workflow

  dbt-compile:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run dbt commands
        run: |
          dbt debug
          dbt deps
          dbt compile
          dbt build
